<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <!-- "Call the Listing", should appear as action button if possible -->
    <item
        android:id="@+id/call_the_listing"
        android:icon="@drawable/ic_phone_black_24dp"
        android:title="action_call_listing"
        app:showAsAction="ifRoom" />

    <!-- "Open Listing in Browser", should appear as action button if possible -->
    <item
        android:id="@+id/open_in_browser"
        android:icon="@drawable/ic_open_in_browser_black_24dp"
        android:title="action_open_in_browser"
        app:showAsAction="ifRoom" />
</menu>